define([],
    function () {

        function AvatarController(avatar, domElement) {
            this.avatar = avatar;
            this.domElement = domElement;
        }

        AvatarController.prototype.init = function () {

        }

        return AvatarController;
    }
);
